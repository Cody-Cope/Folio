import { SettingsForm } from '@/components/profile/settings-form'

export const metadata = {
  title: 'Settings — buildon',
  description: 'Manage your profile and account settings.',
}

export default function SettingsPage() {
  return <SettingsForm />
}
