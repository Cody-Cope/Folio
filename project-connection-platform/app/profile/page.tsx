import { UserProfile } from '@/components/profile/user-profile'

export const metadata = {
  title: 'Profile — buildon',
  description: 'View your public profile and projects.',
}

export default function ProfilePage() {
  return <UserProfile />
}
